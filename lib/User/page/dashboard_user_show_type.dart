import 'package:flutter/material.dart';

class DashboardUserShowType extends StatelessWidget {
  const DashboardUserShowType({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}